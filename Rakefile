# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/chat_server.rb'

Hoe.new('ChatServer', Chatserver::VERSION) do |p|
  # p.rubyforge_name = 'ChatServerx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
