# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/chatter.rb'

Hoe.new('Chatter', Chatter::VERSION) do |p|
  # p.rubyforge_name = 'Chatterx' # if different than lowercase project name
  p.developer('Joshua Clingenpeel', 'joshua.clingenpeel@gmail.com')
end

# vim: syntax=Ruby
